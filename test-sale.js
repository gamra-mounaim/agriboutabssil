import "dotenv/config"; import db, { initDb } from "./src/server/database.js"; import { v4 as uuidv4 } from "uuid"; async function test() { await initDb(); const saleId = uuidv4(); try { await db.prepare("INSERT INTO sales (id, invoice_number, total, subtotal, discount, payment_method, customer_id, customer_name, staff_id, check_number, check_owner) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)").run(saleId, 1, 100, 100, 0, "cash", null, "test", "admin", null, null); console.log("Success"); } catch(e) { console.error(e); } } test();
